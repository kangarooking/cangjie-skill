---
name: {{skill-slug}}
description: |
  {{何时调用 + 何时不调用 + 关键 trigger 信号, ≤300 字}}
trigger_words: [{{中文 trigger 1}}, {{中文 trigger 2}}, {{中文 trigger 3}}, {{en trigger 1}}, {{en trigger 2}}]
source_book: 《{{BOOK_TITLE}}》 {{AUTHOR}}
source_chapter: {{章节}}
tags: [{{tag1}}, {{tag2}}]
related_skills: []    # 阶段 3 填充
---

<!--
  填写说明 (生成最终 skill 时删除本注释块):

  - description: 基于 A2 段浓缩, ≤300 字, 必须含"何时调用 + 何时不调用 + 关键 trigger 信号"。
    好例: "用户在纠结一个决策、列举正面理由却理不出头绪时;或在问'怎么做 X 才能成功'时。不适用于: 纯信息查询、日常琐碎选择。"

  - trigger_words (强制字段, ≥4 条):
    显式列出中英文触发词, 提高 LLM 解析时的触发准确率。
    中文 2-3 条 + 英文 2 条, 覆盖核心概念和典型提问方式。
    例: [逆向思维, 反过来想, 如何避免失败, inversion thinking, think backwards]

  - source_chapter: 书填章节/页码; 视频填时间戳/分 P; 播客填集数。
  - source_book / tags / related_skills 为审计与链接用的扩展字段, 宿主加载 skill 时只读 name + description。

  === darwin rubric 对齐的强制规则 (违反则阶段 4 打回) ===

  [规则 1] E 段必须包含至少 1 个 🔴 CHECKPOINT 视觉标记 (dim4 检查点设计)。
    - CHECKPOINT 后必须有明确动作: "停下来问用户 X" / "展示给用户确认 X" / "等用户回应后再继续"。
    - 仅靠"必须"措辞不算 CHECKPOINT —— LLM 解析时扫描视觉标记。
    - 好例: "🔴 CHECKPOINT — 如果列不出至少 5 条, 停下来问用户补充背景信息, 不要自行猜测。"
    - 坏例: "必须确保列出 5 条。" (无视觉标记, 无明确动作)

  [规则 2] B 段必须包含"Agent 执行反模式"小节 (dim9 反例黑名单)。
    - 至少 3 条"本 skill 激活后不要做的事"。
    - 每条必须有明确动作, 不能只是"注意 X"。
    - 好例: "不要替用户做最终决策——只提供分析框架, 决策权在用户。"
    - 坏例: "注意不要替用户做决策。" (无明确动作)

  [规则 3] A2 段末尾必须包含"快速触发决策树" (dim7 整体架构)。
    - 用 ASCII 决策树格式, 覆盖"不激活"和"激活"两类场景。
    - 决策树必须包含至少 2 个"触发兄弟 skill"的分支 (指向 related_skills)。
    - 例:
      ```
      用户问题
      ├─ 纯知识查询？→ 不激活
      ├─ 明确请求"从失败角度分析"？→ 触发 inversion-thinking
      ├─ 只用 1-2 个维度分析复杂问题？→ ✅ 激活
      └─ 主动寻求更全面分析？→ ✅ 激活
      ```
-->

# {{Skill Title}}

## R — 原文 (Reading)

> {{原文引用, ≤150 字 (英文 ≤100 词), 必须标注章节/页码/时间戳}}
>
> — {{AUTHOR}}, {{CHAPTER}}

---

## I — 方法论骨架 (Interpretation)

{{ 用自己的话重写, 5-15 行。
   读完这段, 一个没读过原书的人应当能理解这个方法论在做什么。
   禁止照搬原文, 禁止堆砌修辞。}}

---

## A1 — 书中的应用 (Past Application)

### 案例 1: {{案例名}}
- **问题**: {{作者遇到了什么}}
- **方法论的使用**: {{作者怎么用这个方法论思考}}
- **结论**: {{得出了什么}}
- **结果**: {{实际发生了什么}}

### 案例 2 (如有)
...

---

## A2 — 触发场景 (Future Trigger) ★

### 用户会在什么情境下需要这个 skill?

1. {{场景 1 — 具体到可识别的情况}}
2. {{场景 2}}
3. {{场景 3}}

### 语言信号 (用户的话里出现这些就应激活)

- "{{典型措辞 1}}"
- "{{典型措辞 2}}"
- "{{典型措辞 3}}"

### 与相邻 skill 的区分

- 与 `{{related-skill-a}}` 的区别: {{ ... }}
- 与 `{{related-skill-b}}` 的区别: {{ ... }}

### 快速触发决策树

```
用户问题
├─ 已有 ≥{{N}} 个维度分析？→ 不激活（用户已有成熟框架）
├─ 纯知识查询/历史学习？→ 不激活
├─ 纯技术调试？→ 不激活
├─ 明确请求"{{兄弟 skill A 的核心动作}}"？→ 触发 `{{sibling-A}}`（兄弟 skill）
├─ 明确请求"{{兄弟 skill B 的核心动作}}"？→ 触发 `{{sibling-B}}`（兄弟 skill）
├─ 只用 1-{{N}} 个维度分析复杂问题？→ ✅ 激活
└─ 主动寻求更全面分析？→ ✅ 激活
```

---

## E — 可执行步骤 (Execution)

当 skill 被激活后, agent 应按以下步骤执行:

1. **{{步骤 1}}**
   - 完成标准: {{如何判断这一步已完成}}

2. **{{步骤 2}}**
   - 完成标准: {{ ... }}

🔴 **CHECKPOINT** — 若 {{X}}, 必须停下来问用户 {{确认什么}}, 不要自行 {{猜测/裁决}}。

3. **{{步骤 3}}**
   - 完成标准: {{ ... }}

<!--
  强制: 至少 1 个 🔴 CHECKPOINT 视觉标记。
  CHECKPOINT 后必须有明确动作（"停下来问用户" / "展示给用户确认"）。
  仅靠"必须"措辞不算 CHECKPOINT —— LLM 解析时扫描视觉标记。
-->

---

## B — 边界 (Boundary) ★

### 不要在以下情况使用此 skill

- {{反场景 1 — 为什么不适用}}
- {{反场景 2}}

### 作者在书中警告的失败模式

- {{ 来自 counter-example-extractor 的对应反例 }}

### 作者的盲点 / 时代局限

- {{ 来自阶段 0 批判阶段 }}

### 容易混淆的邻近方法论

- {{ ... }}

### Agent 执行反模式（本 skill 激活后不要做的事）

- 不要替用户做最终决策——只提供 {{分析/框架/清单}}，决策权在用户
- 不要在用户没确认的情况下跳到步骤 {{N}}——步骤 {{N-1}} 的 CHECKPOINT 必须等用户回应
- 不要在 edge_case 场景强行激活——如果用户只是 {{轻微相关场景}}，先澄清需求
- 不要堆砌不相关的 {{方法论单元}}——每个 {{单元}} 必须写出与当前问题的具体关联，否则删掉
- 不要在信息不足时编造 {{分析内容}}——如果用户对问题背景描述太少，先问清楚再执行步骤 2

---

## 相关 skills (阶段 3 填充)

- depends-on: {{}}
- contrasts-with: {{}}
- composes-with: {{}}

---

## 审计信息

- **验证通过**: V1 ✓ / V2 ✓ / V3 ✓
- **测试通过率**: {{%}} (详见 test-prompts.json)
- **蒸馏时间**: {{DATE}}
